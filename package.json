{
  "private": true,
  "scripts": {
    "build": "flow && babel src -d lib",
    "precommit": "yarn run test && pretty-quick --staged",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "flow-bin": "^0.69.0",
    "flow-typed": "^2.4.0",
    "husky": "^0.14.3",
    "jest": "^22.4.3",
    "prettier": "^1.11.1",
    "pretty-quick": "^1.4.1"
  }
}
